export const environment = {
  production: true,
  apiUrl: 'https://quiz-backend-pink-psi.vercel.app/api'
};

// Verify production environment is loaded
console.log('🔴 PRODUCTION ENVIRONMENT LOADED:', environment);

